{
  "name": "mindar-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy": "npm run build && git add docs && git commit -m \"deploy actualizado\" && git push"
},
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "gh-pages": "^6.3.0",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "three": "^0.150.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
